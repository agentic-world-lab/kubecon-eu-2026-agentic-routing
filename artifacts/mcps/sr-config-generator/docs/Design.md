# Domain Config Generator MCP

I'm developing a MCP that generates the config file for semantic router by rendering a jinja2 template and retuning the result. 
The MCP tool to develop is the domain config generator. 

