---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: llmbackends.edgecloudlabs.edgecloudlabs.com
spec:
  group: edgecloudlabs.edgecloudlabs.com
  names:
    kind: LLMBackend
    listKind: LLMBackendList
    plural: llmbackends
    singular: llmbackend
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.deployment
      name: Deployment
      type: string
    - jsonPath: .spec.model
      name: Model
      type: string
    - jsonPath: .status.backendName
      name: Backend
      type: string
    - jsonPath: .status.results.overallAccuracy
      name: Accuracy
      type: string
    - jsonPath: .status.results.avgResponseTime
      name: Latency(s)
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          LLMBackend is the Schema for the llmbackends API.
          It orchestrates the creation of an AgentGatewayBackend, triggers an MMLU
          evaluation Job, and collects structured results for external agent consumption.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of LLMBackend
            properties:
              apiKeySecretRef:
                description: apiKeySecretRef references the Secret containing the
                  API key for the endpoint.
                properties:
                  key:
                    description: key is the key within the Secret data to use.
                    type: string
                  name:
                    description: name is the name of the Secret in the same namespace.
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              deployment:
                allOf:
                - enum:
                  - local
                  - remote
                - enum:
                  - local
                  - remote
                description: deployment specifies if the model is hosted locally or
                  remotely.
                type: string
              endpoint:
                description: endpoint is the URL of the vLLM-compatible OpenAI API
                  endpoint.
                type: string
              evaluationImage:
                default: fjvicens/mmlu-pro-eval-job:0.2
                description: evaluationImage is the container image to use for the
                  evaluation Job.
                type: string
              model:
                description: model is the name/identifier of the AI model to evaluate.
                minLength: 1
                type: string
            required:
            - deployment
            - model
            type: object
          status:
            description: status defines the observed state of LLMBackend
            properties:
              backendName:
                description: backendName is the name of the created AgentGatewayBackend
                  resource.
                type: string
              conditions:
                description: conditions represent the current state of the LLMBackend
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              jobName:
                description: jobName is the name of the created evaluation Job.
                type: string
              phase:
                description: phase represents the current phase of the evaluation
                  lifecycle.
                enum:
                - ""
                - BackendCreated
                - Evaluating
                - Evaluated
                - Failed
                type: string
              results:
                description: results contains the structured evaluation results.
                properties:
                  avgResponseTime:
                    description: avgResponseTime is the average response time in seconds.
                    type: string
                  categoryAccuracy:
                    additionalProperties:
                      type: string
                    description: categoryAccuracy contains accuracy scores per domain/category.
                    type: object
                  overallAccuracy:
                    description: overallAccuracy is the overall accuracy of the model
                      across all categories.
                    type: string
                  tokensPerSecond:
                    description: tokensPerSecond is the average tokens per second during
                      evaluation.
                    type: string
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
